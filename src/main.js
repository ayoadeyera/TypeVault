/* ==========================================================================
   TypeVault - Vite Entry Point (src/main.js)
   Imports CSS (processed by Vite) and bootstraps the application.
   ========================================================================== */

// CSS imports — Vite handles bundling & minification
import './css/main.css';
import './css/typing.css';
import './css/components.css';

// Application bootstrap — app.js handles DOMContentLoaded internally
import './js/app.js';
